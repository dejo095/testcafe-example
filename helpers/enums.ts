export enum Timeouts {
  '1sec' = 1000,
  '5sec' = 5000,
  '10sec' = 10000
}

export enum ProductsSortOptions {
  NameAsc = 'Name (A to Z)',
  NameDesc = 'Name (Z to A)',
  PriceAsc = 'Price (low to high)',
  PriceDesc = 'Price (high to low)'
}
